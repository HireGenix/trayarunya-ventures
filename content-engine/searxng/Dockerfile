# Self-hosted SearXNG meta-search for the research engine.
# Thin layer over the official image that bakes in our JSON-enabled settings.
FROM searxng/searxng:latest

COPY settings.yml /etc/searxng/settings.yml
